# OpenClaw Agent Connector Configuration
# Copy this file to .env and fill in your values

# Required Settings
RELAY_URL=wss://localhost:8080          # WebSocket URL of the relay server
AGENT_ID=my-openclaw-agent              # Unique identifier for this agent
AGENT_SECRET=your-secret-key-here       # Secret key for agent authentication
AGENT_DISPLAY_NAME=My OpenClaw Agent    # Display name shown to users in Chrome extension

# Optional Reconnection Settings
RECONNECT_DELAY_MS=1000                 # Initial delay before reconnecting (milliseconds)
MAX_RECONNECT_DELAY_MS=30000           # Maximum delay between reconnect attempts
RECONNECT_BACKOFF_MULTIPLIER=1.5       # Multiplier for exponential backoff

# Example configurations:

# For local development with self-hosted relay:
# RELAY_URL=ws://localhost:8080
# AGENT_ID=dev-agent-001
# AGENT_SECRET=dev-secret-123
# AGENT_DISPLAY_NAME=Development Agent

# For production with hosted relay:
# RELAY_URL=wss://relay.openclaw.com
# AGENT_ID=prod-agent-001  
# AGENT_SECRET=secure-production-secret
# AGENT_DISPLAY_NAME=Production OpenClaw Agent