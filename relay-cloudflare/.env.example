# Example environment configuration for OpenClaw Chrome Relay
# Copy this file to .dev.vars for local development

# JWT Secret - Use a strong, random 32+ character string
# Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-key-here-32-characters-minimum

# Agent Secret - Shared secret for agent authentication
# Generate with: openssl rand -base64 24
AGENT_SECRET=your-agent-secret-key-here

# Optional: Custom domain for production
# CUSTOM_DOMAIN=relay.yourdomain.com

# Development settings (automatically used in dev mode)
# CLOUDFLARE_ACCOUNT_ID=your-account-id
# CLOUDFLARE_API_TOKEN=your-api-token